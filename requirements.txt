# Core
# Versions chosen to ship cp311 - cp314 wheels (Python 3.11 - 3.14).
# Bumping pydantic past 2.12 is required because earlier pydantic-core
# bundles ship a PyO3 that caps out at Python 3.13 and tries to source-build.
python-dotenv==1.0.1
pydantic==2.13.3
pydantic-settings==2.14.0
PyYAML==6.0.3           # 6.0.3 (Sep 2025) is the first PyYAML release with cp314 wheels.
ruamel.yaml==0.18.6     # comment-preserving YAML round-trip (auto-watchlist daily write-back)
typer==0.12.5
click==8.1.8           # typer 0.12.5 is incompatible with click 8.2+ (see fastapi/typer#1145).
rich==13.9.4
loguru==0.7.2

# Data + math
# Versions chosen to ship cp314 wheels (Python 3.14) as well as cp310-cp313.
# pandas 2.3.3 (Sep 2025) is the first release with Python 3.14 wheels.
# numpy 2.4.3+ (Oct 2025) ships cp314 wheels.
pandas==2.3.3
numpy==2.4.4
yfinance==1.3.0        # 1.x switched to curl_cffi to bypass Yahoo's anti-bot;
                       # versions <= 0.2.50 throw "Expecting value: line 1 col 1"
                       # against the current Yahoo response shape.

# Scheduling + concurrency
APScheduler==3.10.4
pytz==2024.2

# Cache
redis==5.2.0

# Dashboard
# streamlit < 1.52 pins pyarrow < 22, and pyarrow < 22 has no cp314 wheels.
streamlit==1.52.0
plotly==5.24.1

# Brokers (optional - install only what you use)
kiteconnect==5.0.1     # Zerodha (REST + KiteTicker WebSocket)
# dhanhq==2.0.2        # Uncomment to use Dhan
pyotp==2.9.0           # TOTP for daily Kite login bootstrap

# (scipy was removed — bot/backtest_advanced.py uses only numpy + pandas.)

# Research agent (optional)
openai==2.32.0         # 1.55 passes proxies= to httpx; httpx>=0.28 removed it.
                       # Bumping fixes: "Client.__init__() got an unexpected
                       # keyword argument 'proxies'".
feedparser==6.0.11
beautifulsoup4==4.12.3
requests==2.32.3

# Testing
pytest==8.3.3
